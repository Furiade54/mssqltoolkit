// Temporary stub to satisfy a stray import in preview environment.
// If your app later defines real game types, replace this with actual implementations.
export class Enemy {}
export type EnemyStats = {
  hp?: number;
  attack?: number;
};